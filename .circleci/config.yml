version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.6

    environment:
    - NODE_ENV: development

    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run test
