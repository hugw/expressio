version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.6

    environment:
      - NODE_ENV: test
      - FOO: BAR
      - APP_NAME: Demo

    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run test
